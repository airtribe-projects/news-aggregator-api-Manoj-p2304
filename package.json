{
  "name": "news-aggregator-api",
  "version": "1.0.0",
  "description": "A RESTful API for a personalized news aggregator (Airtribe Backend Engineering Launchpad - Assignment 2)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "pretest": "node -e \"if (process.version.slice(1).split('.')[0] < 18) { console.error('Unsupported Node.js version, please use version 18'); process.exit(1); }\"",
    "test": "tap test/*.js --disable-coverage"
  },
  "author": "Airtribe",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.4",
    "supertest": "^6.3.4",
    "tap": "^18.6.1"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
